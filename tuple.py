# tuple are data structures which are imputable (u cant change values stored in tuples)
# in list you can able to make change or replace some items in list using index
# in tuples we can not store values after once they already stored in the tuples


fruits = ("Apple", "Mango ", "Peech")
print(fruits)
print(fruits[0])
# fruits[0] = "pineapple" we can not reassign the value
fruits2 = "Apple", "Mango ", "Peech"
print(fruits2)